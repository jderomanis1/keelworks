# Deromanis

AI specialist crew. Human in the loop.

Public marketing site (vanilla HTML/CSS/JS). GitHub Pages from `main` via Actions.

Brand: **Deromanis** · style cue `deromanis.ai` (style only until DNS verified).

Contact: Text `602.568.5508` · Email us (mailto only; address not shown in page copy).

## Public allowlist (`_site`)

- `index.html`
- `css/`
- `js/`
- `assets/` (including self-hosted OFL fonts)

Do not deploy: `prototype/`, `SPEC.md`, `STATE.md`, positioning/legal/ux docs.
