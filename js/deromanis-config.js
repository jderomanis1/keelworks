/* Public configuration only. NEVER place API keys or confidential material here.
 * See DEROMANIS-LAUNCH.md before enabling hosted voice. Empty values keep it off.
 */
window.DEROMANIS_CONFIG = Object.freeze({
  voice: { agentId: "" },
  intro: { audioPath: "", transcript: "" }
});
